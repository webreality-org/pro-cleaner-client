import { SearchParamsProps } from '@/types';

export default async function Home({ searchParams }: SearchParamsProps) {
  return (
    <>
      <div className=" flex min-h-[70vh] flex-col justify-between">home</div>
    </>
  );
}
